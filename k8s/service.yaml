apiVersion: v1
kind: Service
metadata:
  name: forge-api
  namespace: forge
spec:
  selector:
    app.kubernetes.io/name: forge-api
    forge.release: stable
  ports:
    - name: http
      port: 80
      targetPort: http